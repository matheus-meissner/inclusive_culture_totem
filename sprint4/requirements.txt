# Sprint 4 — Tótem Inteligente Inclusivo
# Instale com: pip install -r requirements.txt

# Core
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Dashboard
streamlit>=1.32.0

# Visualizações (usadas pelo ML train)
matplotlib>=3.7.0

# Banco de dados (stdlib Python, sem instalação necessária)
# sqlite3 — já incluso no Python

# ---- Módulos opcionais (Sprint 4 - funcionalidades avançadas) ----
# Descomente se quiser câmera real ou microfone real:

# Visão computacional (câmera real via OpenCV):
# opencv-python>=4.8.0

# Reconhecimento de voz (microfone real):
# SpeechRecognition>=3.10.0
# pyaudio>=0.2.13          # driver de microfone (Linux: sudo apt install portaudio19-dev)
# openai-whisper>=20231117  # reconhecimento offline (requer torch)

# ---- Notas ----
# Os módulos de visão e voz funcionam 100% em modo simulado sem as libs opcionais.
# Para ativar câmera real: descomente opencv-python e rode: python main.py vision-bulk --use-camera
# Para ativar microfone: descomente SpeechRecognition + pyaudio
